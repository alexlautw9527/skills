# Skills 專案詞彙表

本文件定義新建或修改 skill 時使用的章節詞彙。所有文件先依章節實際回答的問題選詞，再決定是否需要該章節；不為了套用模板加入沒有內容的區塊。

- `Skill`：可重複使用的工作指引，說明適用情境、工作方法、判斷條件與交付結果。
    - 使用方式：用於描述一類可重複執行的工作，不用來記錄單一專案的現況。

- `Goal`：要達成的結果。
    - 使用方式：說明一項工作、計畫或交付物完成後應呈現的狀態。

- `Non-goal`：本次工作、計畫或交付物刻意不達成的結果或能力。
    - 使用方式：用於界定範圍，避免將未包含的需求誤認為交付承諾。

- `Glossary Map`：列出各範圍 glossary 的位置與適用範圍的中央索引。
    - 使用方式：在詞彙分散於多個範圍時，協助讀者找到應查閱的 glossary，不重複記錄詞彙定義。

- `Scope`：本次工作、模組或交付物負責的範圍，以及明確排除的內容。
    - 使用方式：用 `In Scope` 與 `Out of Scope` 列出包含及排除項目。

- `Inputs`：開始工作前需要的材料、來源與已知限制。
    - 使用方式：材料缺漏時，列出會受影響的判斷與需要補充的資訊。

- `Questions`：尚未確認、會影響目前判斷的資訊，以及為了繼續工作而暫定採用的前提。
    - 使用方式：只列出會影響範圍、優先順序、責任歸屬或結果正確性的項目，並說明仍需確認的內容。

- `Workflow`：完成工作所需的步驟與順序。
    - 不使用：不重複列出規則或交付內容。

- `Constraints`：執行 skill 時不可違反的條件。
    - 使用方式：記錄禁止事項、必要限制、完成後必須成立的條件，以及遇到特定情況時必須採取的處理方式。

- `Invariant`：每次一項操作完成後，資料或系統狀態都必須成立的條件。
    - 使用方式：描述可用來判斷操作結果是否正確，以及結果是否能安全交給下一個操作使用的條件。

- `Principle`：建議採用的做法或判斷方向，但不構成必須遵守的條件。
    - 使用方式：在有合理例外、需要依情境取捨時，說明預設應採取的方向。
    - 不使用：不能違反的限制、禁止事項與完成後必須成立的條件，改放在 `Constraints`。

- `sbe`：以具體情境、規則與驗收結果描述系統行為的規格文件。
    - 使用方式：在實作與設計前釐清需求、例外情境與驗收條件。

- `ui design`：說明畫面、互動與視覺呈現的設計來源，例如 Figma 檔案。
    - 使用方式：作為 ui spec、system design 或 plan 的輸入；只引用本次功能相關的畫面與互動。

- `ui spec`：從 ui design 整理出的結構化介面規格，保留會影響實作與驗證的畫面、互動、狀態與標註。
    - 使用方式：用於讓設計意圖能被實作與 review，不重複描述程式碼可直接查得的細節。

- `system design`：將行為需求、既有系統與技術限制整理為責任分工、資料處理方式與高修改成本決策的設計文件。
    - 使用方式：當狀態、介面、資料、跨系統協作或失敗處理需要先決定時建立；合併前只保留日後修改仍需理解的內容。

- `plan`：依既有系統位置與已確認的行為、設計決策，排定可實作與驗證的交付順序。
    - 使用方式：需要查證實作位置、跨 repository 範圍、既有模式或需求追溯關係時建立；plan 包含 slice 切分。

- `slice`：以可觀察行為切出的交付單位。
    - 使用方式：每個 slice 完成後可以獨立實作、驗證與 review。
    - 不使用：不以 UI、API、資料庫或測試等技術分層命名。

- `task`：由同一位開發者或 coding agent 連續完成、可單獨 review 的工程改動單位。
    - 使用方式：將已確認的 slice 與 plan 拆成能執行、驗證與 review 的工作。

- `Story`（sbe）：以角色、目標與目的描述的一項需求；使用 `As a <角色>, I want <目標>, so that <目的>` 格式。

- `Feature`（sbe）：Gherkin 文件中描述一項能力的最上層單位；包含一個或多個 Rule 或 Scenario。

- `Rule`（sbe）：Feature 下的一條業務規則或 acceptance criterion；由一個或多個 Scenario 說明。

- `Scenario`（sbe）：描述一組具體前提、操作與結果的 Gherkin 範例，用來說明一條 Rule。

- `Risks`：可能造成錯誤判斷、錯誤結果或需要額外處理的情況。
    - 使用方式：記錄已知失敗模式、例外情境、失敗處理方式，以及程式碼表面看不出來、卻會影響維護判斷的隱含邏輯。

- `Output`：完成後要交付的內容。
    - 使用方式：交付物有固定欄位或格式時，在本節下說明格式或提供模板。

- `Validation`：如何取得證據並確認交付物、程式行為或修改結果符合要求。
    - 使用方式：交付前的檢查清單、測試與人工檢查都放在此節。

- `Stop Conditions`：應停止工作或停止擴大範圍的條件。
    - 使用方式：在沒有新增可驗證結果時，停止持續探索或增加內容。

- `Map`：既有程式碼的現況地圖。
    - 使用方式：指出入口、實作位置、資料流、相依關係、可沿用模式與閱讀路徑，讓實作者知道應從哪裡開始理解或修改。

- `Model`：系統為何以目前方式運作的解釋。
    - 使用方式：說明狀態持有者、資料或物件關係、生命週期與跨檔案協調方式。

- `Rationale`：現有設計為何刻意維持，以及替代做法會造成什麼後果。
    - 使用方式：只保留仍會影響維護或修改判斷的背景。

- `Shared Concerns`：兩個以上 slice 共同依賴的基礎能力、資料模型、規格理解或實作邊界。
    - 使用方式：說明哪些 slice 依賴它、它提供什麼共同前提，以及各 slice 仍各自負責什麼。
    - 不使用：尚未具備、必須先完成才能解除阻礙的工作，改放在 `Prerequisites`。

- `Prerequisites`：尚未具備、必須先完成才能讓 slice 開始或完成的工作。
    - 使用方式：記錄無法獨立形成呼叫端可驗證行為的技術前置條件，例如資料遷移、基礎設施設定或外部介面準備。
    - 不使用：已存在且由多個 slice 共同依賴的基礎，改放在 `Shared Concerns`。

- `Coverage`：需求與交付單位的追溯關係。
    - 使用方式：列出每個需求由哪些 slice 處理，或明確標記不處理的原因。
